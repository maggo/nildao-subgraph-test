{
  "name": "nildao-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nildao-test",
    "create-local": "graph create --node http://localhost:8020/ nildao-test",
    "remove-local": "graph remove --node http://localhost:8020/ nildao-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nildao-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
